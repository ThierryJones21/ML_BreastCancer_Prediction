# ML_BreastCancer_Prediction: 
Wisconsin DataSet predicts benign or maligant tumors with 3 different models

# Pip Installs: 
pip install seaborn
pip install pandas
pip install sklearn
pip install numpy
